# Аудит проекта Country Decision Atlas — список проблем и их устранение

**Дата аудита:** 2026-07-20 · **Дата устранения:** 2026-07-20 · **Ветка:** `main`
**Статус: все 11 находок закрыты.** 7 исправлены кодом, 3 инспектированы
глубже и оказались либо уже верными по факту, либо уже задокументированными
(правка была бы вредной или избыточной — см. §6), 1 закрыта документацией.

**Как читать.** Каждая находка оформлена одинаково:
**Проблема** → **Где** (файл:строка) → **Как чинить** → **Важно знать**, и
теперь дополнена блоком **✅ Устранено**/**↺ Пересмотрено** с итогом.
Severity: 🔴 High (ломает или небезопасно) · 🟡 Medium (заметно вредит) ·
🟢 Low (косметика/долг) · ⚪ Info (осознанное решение, не баг).

**Метод исходного аудита.** Статический анализ + запуск приложения (dev и
prod-сборка, Docker-стек с реальной БД и посеянными данными) + авто-прогон
всех 36 достижимых маршрутов на desktop/mobile + прогон всех линтеров и
тестов. Каждое срабатывание грепов проверялось по контексту вручную —
ложные срабатывания в список **не попали** (см. §4).

**Метод устранения.** Каждая находка чинилась, проверялась (typecheck /
lint / тесты / живой браузер / go vet+test, где применимо) и коммитилась
по отдельности. Три находки (I18N-2, CFG-1, I18N-3) при более глубоком
расследовании оказались основаны на неполной картине первого прохода —
правки по ним **не вносились**, и это объясняется честно в §6, а не
скрывается.

---

## 0. Оглавление находок

| # | Sev | Область | Проблема | Итог |
|---|-----|---------|----------|------|
| [A-1](#a-1) | 🟡 | a11y | 14 полей формы без доступного имени | ✅ Исправлено |
| [A-2](#a-2) | 🟡 | a11y | 8 `<FieldLabel>` без `htmlFor` — подпись не связана с полем | ✅ Исправлено |
| [UX-1](#ux-1) | 🟡 | UX | Оптимистичные мутации откатываются **молча** | ✅ Исправлено |
| [I18N-1](#i18n-1) | 🟡 | i18n | Нет `[locale]/not-found.tsx` → EN/ES видят русскую 404 | ✅ Исправлено |
| [SEC-1](#sec-1) | 🟡 | Безопасность | Порты dev-стека на `0.0.0.0`: redis/mongo **без auth** | ✅ Исправлено |
| [I18N-2](#i18n-2) | 🟢 | i18n | Захардкожена локаль `"ru"` в таймстампах админки | ↺ Пересмотрено — правка не нужна |
| [ARCH-1](#arch-1) | 🟢 | Архитектура | `sys.path`-хак в worker тянется во внутренности `apps/api` | ✅ Исправлено |
| [NOTIF-1](#notif-1) | 🟢 | i18n | Тексты Telegram-уведомлений только по-русски | ✅ Исправлено |
| [CFG-1](#cfg-1) | 🟢 | Конфиг | Неиспользуемые mypy-override в `pyproject.toml` | ↺ Пересмотрено — правка не нужна |
| [I18N-3](#i18n-3) | ⚪ | i18n | `/internal/**` полностью по-русски | ↺ Пересмотрено — уже задокументировано |
| [ARCH-2](#arch-2) | ⚪ | Процесс | `apps/worker` не упомянут в compose/CI/доках | ✅ Закрыто документацией |

---

## 1. Находки

<a id="a-1"></a>
### A-1 🟡 14 полей формы без доступного имени

**Проблема.** У элементов нет ни `aria-label`, ни `id` со связанным
`<label htmlFor>`, ни обёртки `<label>`. Скринридер объявляет их как
безымянные («edit text», «combo box»), пользователь не понимает, что
вводить. Часть — чекбоксы в чек-листах поездок, то есть основной сценарий.

**Где.**
```
apps/web/src/features/admin-users/UsersAdminView.tsx:90            <select>
apps/web/src/features/author-metrics/AuthorMetricsStudioView.tsx:186 <input>
apps/web/src/features/community/CommunityCountryBlock.tsx:426      <input>
apps/web/src/features/community/CommunityCountryBlock.tsx:437      <textarea>
apps/web/src/features/community/CommunityCountryBlock.tsx:467      <select>
apps/web/src/features/community/CommunityCountryBlock.tsx:491      <textarea>
apps/web/src/features/community/CommunityCountryBlock.tsx:534      <input>
apps/web/src/features/community/CommunityCountryBlock.tsx:555      <textarea>
apps/web/src/features/migration-board/MigrationBoardDetailView.tsx:153 <textarea>
apps/web/src/features/migration-board/MigrationBoardDetailView.tsx:186 <textarea>
apps/web/src/features/trips/TripChecklist.tsx:37                   <input type=checkbox>
apps/web/src/features/trips/TripChecklist.tsx:105                  <input>
apps/web/src/features/trips/TripChecklist.tsx:162                  <input>
apps/web/src/features/trips/TripReminders.tsx:91                   <input>
```

**Как чинить.** Любой из трёх способов, по ситуации:
1. `aria-label={t("...")}` — быстрее всего там, где подписи visually нет
   (чекбоксы в `TripChecklist`);
2. `id` на поле + `htmlFor` на подписи — если подпись уже нарисована рядом
   (`UsersAdminView.tsx:87` — там подпись лежит в `<span>`, её достаточно
   заменить на `<FieldLabel htmlFor="...">`);
3. обернуть контрол в `<label>` — как уже сделано в
   `GlossaryFilters.tsx` (хороший образец в проекте).

**Важно знать.** В проекте **уже есть правильный паттерн** — смотрите
`apps/web/src/features/glossary/GlossaryFilters.tsx:31-41`
(обёртка `<label>`) и `DecisionWeightSlider.tsx:35` (`aria-label={label}`).
Стоит выровнять остальные по ним, а не изобретать новый.

**✅ Устранено.** Все 14 полей размечены: `UsersAdminView` — `<span>` →
`<label htmlFor>`; `AuthorMetricsStudioView` — то же, `id` на строку по
`country.slug`; `CommunityCountryBlock` — 6 полей получили `id`/`htmlFor`
(попутно закрыло 6 из 8 пунктов A-2, те же поля); `MigrationBoardDetailView` —
2 textarea получили `aria-label` (переиспользованы существующие ключи
`contactRequestKicker`/`reportKicker`, новых строк не заводил);
`TripChecklist` — чекбокс обёрнут в `id`+`<label htmlFor>`, 2 input —
`aria-label`; `TripReminders` — `aria-label` на `datetime-local` (добавлен
новый ключ `remindAtLabel` в `en`/`ru`/`es`, проверено `i18n_parity_check.py`
→ 1046 ключей совпадают). Проверено живьём в браузере: все `id`↔`label[for]`
связи резолвятся, тексты подставляются.

---

<a id="a-2"></a>
### A-2 🟡 8 `<FieldLabel>` без `htmlFor` — подпись не связана с полем

**Проблема.** `FieldLabel` рендерит настоящий `<label htmlFor={htmlFor}>`,
но проп **опциональный**. Когда его не передают, а у соседнего поля нет
`id`, получается `<label>` без связи: подпись видна глазами, но
программно с полем не сцеплена. Скринридер поле не назовёт, и клик по
подписи не сфокусирует поле.

**Где.**
- `packages/ui/src/primitives/Field.tsx:20-40` — источник (`htmlFor?`).
- `apps/web/src/features/community/CommunityCountryBlock.tsx` — например
  строки 425/426 (`<FieldLabel>` + `<input>` — соседи, без `id`).
- `apps/web/src/features/decision-run/DecisionRunForm.tsx`.

Масштаб: **8 использований из 80** — остальные 72 `htmlFor` передают
корректно, то есть это точечная недоделка, а не системная.

**Как чинить.** Проставить пару `htmlFor`/`id`:
```tsx
<FieldLabel htmlFor="community-question-title">{t("questionTitleLabel")}</FieldLabel>
<input id="community-question-title" ... />
```

**Важно знать.** Радикальнее и надёжнее — сделать `htmlFor` **обязательным**
в типе `FieldLabel`. Тогда TypeScript сам найдёт все 8 мест и не даст
завести новые. Это одна правка в `Field.tsx` + 8 подстановок, и проблема
закрывается навсегда.

**✅ Устранено.** Сделано именно так — `htmlFor` в `FieldLabel` стал
обязательным пропом, `tsc` тут же указал все 8 мест. 6 в
`CommunityCountryBlock` получили `id`/`htmlFor`. Оставшиеся 2 (в
`DecisionRunForm.tsx`) оказались не полем-и-подписью, а **заголовками над
группой** контролов (радио-группа сценария, список чекбоксов
стран-кандидатов) — `<label>` физически не может описывать больше одного
контрола. Для них добавлен новый примитив `FieldGroupLabel` (не `<label>`,
обычный текстовый заголовок с `id`) + `role="group" aria-labelledby="..."`
на контейнер группы. Проверено живьём: `label[for="origin-select"]` и
`role="group"[aria-labelledby="decision-candidates-label"]` резолвятся
корректно на Шаге 2 мастера подбора.

---

<a id="ux-1"></a>
### UX-1 🟡 Оптимистичные мутации откатываются молча

**Проблема.** Мутации применяют изменение оптимистично, а при ошибке
**просто возвращают кэш назад** — без какого-либо уведомления. С точки
зрения пользователя: нажал «в избранное» — звёздочка загорелась и через
мгновение сама погасла, без объяснений. Выглядит как глюк интерфейса, а
не как «сервер отверг запрос».

**Где.**
- `apps/web/src/entities/watchlist/api.ts:99-103` — `onError` только
  `setQueryData(previous)`, тоста нет.
- `apps/web/src/entities/watchlist/api.ts:151` — то же для
  notification-preference.
- `apps/web/src/entities/trips/api.ts:175`, `:251` — тот же паттерн.
- `apps/web/src/app/providers.tsx` — у `QueryClient` **не задан**
  `MutationCache` с общим `onError`, то есть глобального «подхвата» тоже
  нет.
- Вызывающая сторона, например
  `apps/web/src/features/country-catalog/WatchlistStar.tsx:48` — просто
  `toggle.mutate(...)`, ошибку не смотрит.

**Как чинить.** Лучше централизованно, одним местом на всё приложение —
в `providers.tsx`:
```tsx
import { MutationCache } from "@tanstack/react-query";
import { toast } from "@country-decision-atlas/ui";

new QueryClient({
  mutationCache: new MutationCache({
    onError: (error) => toast.error(extractMessage(error)),
  }),
  defaultOptions: { /* как сейчас */ },
});
```
Тогда откат останется в локальных `onError`, а сообщение пользователю
появится само во всех мутациях сразу.

**Важно знать.** Инфраструктура для этого **уже есть**: `Toaster` смонтирован
в `apps/web/src/app/layout.tsx`, а `toast` экспортируется из
`@country-decision-atlas/ui`. Добавлять зависимости не нужно.

**✅ Устранено — точечно, не глобальным `MutationCache`.** Проверка перед
правкой показала, что глобальный перехват дал бы **двойные тосты**: 15
файлов фич уже сами вызывают `toast.error` в `try/catch` вокруг
`mutateAsync`, и `MutationCache.onError` срабатывал бы для них ВТОРОЙ раз
параллельно. Вместо этого `toast.error(...)` добавлен точечно в 4
конкретных `onError` (ровно те, что были в находке): 2 в
`entities/watchlist/api.ts`, 2 в `entities/trips/api.ts`. Сообщение берётся
через новый `mutationErrorMessage()` в `shared/api/http.ts` — переиспользует
уже существующий паттерн locale-aware сообщений (`currentPathLocale()`,
как у сетевых ошибок) вместо новых зависимостей.

---

<a id="i18n-1"></a>
### I18N-1 🟡 Нет `[locale]/not-found.tsx` → EN/ES видят русскую 404

**Проблема.** Единственная страница 404 — корневая, с **захардкоженным
русским** текстом. Внутри `[locale]`-дерева своей `not-found.tsx` нет,
поэтому англо- и испаноязычный пользователь на несуществующем адресе
получает «Страница не найдена / На главную» по-русски.

**Где.**
- `apps/web/src/app/not-found.tsx:11-14` — хардкод RU.
- `apps/web/src/app/[locale]/layout.tsx:35` — вызывает `notFound()` при
  неизвестной локали; ближайшей границы внутри `[locale]` нет, поэтому
  срабатывает корневая.
- Для сравнения: `apps/web/src/app/[locale]/error.tsx` **уже** локализован —
  то есть для ошибок паттерн соблюдён, а для 404 нет.

**Как чинить.** Добавить `apps/web/src/app/[locale]/not-found.tsx`, который
берёт тексты через `useTranslations`/`getTranslations` (он рендерится
внутри `NextIntlClientProvider`, переводы доступны). Корневую
`not-found.tsx` **оставить как есть** — она нужна как запасной вариант.

**Важно знать.** Комментарий в корневой `not-found.tsx` объясняет, почему
там нет next-intl: она может отрисоваться для `/internal/**` и для путей
вообще без локали. Это **верно** — не убирайте её и не тащите туда
переводы; нужна именно **дополнительная** страница внутри `[locale]`.

**✅ Устранено — плюс более глубокая находка по ходу правки.** Просто
добавить `[locale]/not-found.tsx` оказалось недостаточно: в App Router
вложенный `not-found.tsx` срабатывает **только** при явном вызове
`notFound()` изнутри дерева сегмента, а для произвольного несуществующего
URL (нет ни одного `page.tsx`, который бы совпал) Next.js вообще не заходит
в `[locale]`-layout и сразу использует корневую границу — проверено живьём:
после добавления только файла `/en/несуществующая-страница` всё ещё
показывал русский текст. Добавлен `[locale]/[...rest]/page.tsx`
(catch-all), который явно вызывает `notFound()` изнутри `[locale]`-дерева —
это и делает вложенную границу достижимой. Проверено live на 5 сценариях:
`/en/...` → English, `/es/...` → Español, `/ru/...` → русский,
`/internal/несуществующая` → русский (не тронут, как и планировалось),
`/fr/...` (невалидная локаль) → English (осмысленный fallback на
`defaultLocale`, не краш).

---

<a id="sec-1"></a>
### SEC-1 🟡 Порты dev-стека открыты на `0.0.0.0`, redis и mongo без auth

**Проблема.** Инфраструктурные сервисы публикуются в форме `"6379:6379"`,
что в Docker означает привязку ко **всем интерфейсам**, а не к localhost.
При этом:
- **redis** — вообще без `requirepass`;
- **mongo** — без `MONGO_INITDB_ROOT_*` и без `--auth`;
- **postgres** — с паролем по умолчанию `change-me`.

В любой недоверенной сети (коворкинг, кафе, общий офисный Wi-Fi) любой
сосед по подсети может подключиться к вашим базам и прочитать/изменить
данные.

**Где.** `docker-compose.yml`:
```
строка 24  postgres  "${POSTGRES_PORT:-5433}:5432"
строка 36  redis     "6379:6379"        # без requirepass
строка 119 redpanda  "19092:19092"
строка 132 mongo     "27017:27017"      # без auth
строка 160 notifier  "8081:8081", "9090:9090"
```

**Как чинить.** Привязать к loopback:
```yaml
ports:
  - "127.0.0.1:6379:6379"
  - "127.0.0.1:27017:27017"
  - "127.0.0.1:${POSTGRES_PORT:-5433}:5432"
```

**Важно знать.**
- На работу стека это **не влияет**: контейнеры общаются между собой по
  внутренней сети compose (по именам сервисов), а не через опубликованные
  порты. Инструменты на хосте (psql, `apply_migrations.py`, dev-сервер
  Next.js) продолжат ходить на `localhost`.
- **Сломает** единственный сценарий: доступ к этим базам с **другой
  машины/устройства**. Если вы так делаете — оставьте как есть либо
  привяжите к конкретному интерфейсу.
- Прод это **не затрагивает**: `apps/api/app/core/config.py:81-95`
  (`_forbid_default_secrets_in_production`) не даёт приложению стартовать в
  production с дефолтными секретами. Проблема ровно про dev-машину.

**✅ Устранено.** Все 8 портов (postgres, redis, оба порта redpanda, mongo,
оба порта notifier) привязаны к `127.0.0.1`. `api`-порт (8000) намеренно
**не трогал** — за ним стоит настоящая прикладная авторизация, это другая
категория риска, не «сырая БД без пароля». Проверено live: контейнеры
пересозданы (`docker ps` → `127.0.0.1:5433->5432/tcp`,
`127.0.0.1:6379->6379/tcp`, `api` остался на `0.0.0.0:8000`), после чего
`apply_migrations.py` и `curl localhost:8000/health` (200) отработали как
раньше — инструменты хоста доступ не потеряли.

---

<a id="i18n-2"></a>
### I18N-2 🟢 Захардкожена локаль `"ru"` в таймстампах админки

**Проблема.** Время в журнале событий панели пересчёта форматируется
жёстко по-русски, независимо от языка интерфейса.

**Где.** `apps/web/src/features/admin-recompute/RecomputePanelView.tsx:76`
и `:85` — `new Date().toLocaleTimeString("ru")`.

**Как чинить.** Взять текущую локаль и прокинуть её в форматирование:
```tsx
const locale = useAppLocale();
// ...
timestamp: new Date().toLocaleTimeString(locale),
```

**Важно знать.** В проекте уже есть готовые хелперы —
`apps/web/src/shared/lib/format.ts` (`formatDateTime(value, locale)`) и
`apps/web/src/shared/lib/useAppLocale.ts`. Лучше использовать их, а не
звать `toLocaleTimeString` напрямую. Это не баг гидратации: код внутри
обработчика события, не в рендере.

**↺ Пересмотрено — правка не нужна.** Перед правкой проверил
`apps/web/src/app/internal/layout.tsx` полностью (в первом проходе
аудита не читал его целиком) — там `NextIntlClientProvider` **жёстко
захардкожен на `locale="ru"`** («owner-confirmed scope boundary», см. §5
пересмотренного I18N-3). Значит `useAppLocale()` в этом дереве **всегда**
вернёт `"ru"`, и замена `toLocaleTimeString("ru")` на
`toLocaleTimeString(locale)` не изменила бы поведение ни на бит — чисто
косметическая замена без функционального смысла. Не стал вносить
бесполезную правку.

---

<a id="arch-1"></a>
### ARCH-1 🟢 `sys.path`-хак в worker тянется во внутренности `apps/api`

**Проблема.** Worker вычисляет корень репозитория **четырьмя вложенными**
`os.path.dirname` и вручную подставляет `apps/api` в `sys.path`, чтобы
импортировать `app.services.translation_jobs`. Работает, но: (1) молча
сломается, если файл переедет на другой уровень вложенности; (2) worker
жёстко завязан на внутренний сервисный слой API.

**Где.** `apps/worker/country_decision_atlas_worker/main.py:6-11`, затем
импорты внутри функции — строки 21-22.

**Как чинить.** Варианты по возрастанию усилий:
1. Запускать worker как модуль установленного пакета
   (`pip install -e .` уже настроен: `pyproject.toml` объявляет
   `apps/worker` в `[tool.setuptools.packages.find]`) и убрать хак;
2. вынести используемую логику в `packages/shared`, чтобы и API, и worker
   импортировали её как общий пакет, а не лазили друг к другу.

**Важно знать.** Сервис **рабочий** — я его запускал: `--help` отрабатывает,
`translation-jobs --dry-run --limit 2` даёт `processed=0` (корректно при
пустой очереди), тесты `tests/test_translation_jobs_worker.py` — 30 штук,
все зелёные. Это долг, а не поломка; чинить можно спокойно.

**✅ Устранено — вариант 2 отклонён, применён «улучшенный вариант 1».**
Проверка перед правкой показала, что `apps/api/app` в принципе **не
упакован** для дистрибуции (`pyproject.toml`'s `packages.find` ловит
только `country_decision_atlas_*`-пакеты; `import app.services...` без
хака падает даже при установленном editable-пакете) — значит сам хак
**обязателен**, удалить его целиком (вариант 1 «как есть») было бы
регрессией. Вместо этого хрупкая цепочка из 4 `os.path.dirname` заменена
на поиск корня репозитория по маркеру (`pyproject.toml`) — устойчиво к
переносу файла на любую глубину, с громким `RuntimeError` вместо тихой
поломки, если маркер не найден. Проверено live end-to-end: `python
apps/worker/main.py` (help) и `translation-jobs --dry-run --limit 2` с
поднятым Postgres → `processed=0 completed=0 failed=0`, exit 0.

---

<a id="notif-1"></a>
### NOTIF-1 🟢 Тексты Telegram-уведомлений только по-русски

**Проблема.** Сообщения пользователю формируются с русскими строками
без учёта языка получателя, хотя интерфейс поддерживает 3 локали.

**Где.** `apps/notifier/internal/telegram/client.go:63-79` (`FormatMessage`),
дисклеймер — строка 12.

**Как чинить.** Прокинуть локаль получателя в событие/подписку и выбрать
шаблон по ней. Потребует поля локали в подписке
(`apps/notifier/internal/mongo/subscriptions.go`) и в доменном событии.

**Важно знать.** Это заметный объём работы (схема события + хранилище +
шаблоны), а notifier сейчас не входит в i18n-контур интерфейса. Разумно
отложить до момента, когда реально появятся неруглоязычные получатели.

**✅ Устранено — полностью, с бóльшим охватом, чем изначально описано.**
При реализации выяснилось, что «только тексты уведомлений» —
недооценка: **весь бот** (справка, ответы на `/subscribe`,
`/unsubscribe`, `/list`, `/web_link`) был захардкожен по-русски, не
только `FormatMessage`. Источник локали — реальный `language_code`,
который Telegram присылает в каждом апдейте (zero-friction, не нужны
новые команды/UI):

- новый пакет `internal/locale` — резолвит BCP-47 код (`en-US`, `es-MX`)
  в один из `en`/`ru`/`es`, дефолт `ru` (сохраняет прежнее поведение);
- `TelegramUser.LanguageCode` (новое поле) → на каждом взаимодействии с
  ботом `internal/telegram/bot.Handler` резолвит и сохраняет локаль через
  новый `TelegramIdentityRepository.SetLocale` — **не** через
  `CreateSubscription` (тот путь также вызывается по gRPC от backend'а без
  единого Telegram-апдейта, там языка попросту нет — сознательно
  разделил на два независимых пути, не тронул сигнатуру
  `CreateSubscription`, никакие из ~15 существующих вызовов в тестах не
  сломались);
- `internal/telegram/bot/messages.go` — каталог из 14 строк на 3 локали;
  `ru`-вариант побайтово совпадает с прежним хардкодом;
  `internal/telegram/client.go::FormatMessage`/`DisclaimerFor` — то же
  для текста уведомлений о событиях;
  `internal/notifier/handler.go` — форматирование сообщения перенесено
  **внутрь цикла по получателям** (было один раз на всё событие) с
  локалью каждого получателя, взятой через `GetLocale`.

Во время реализации собственный интеграционный тест поймал реальный баг
в тестовом хелпере (`notifier/integration_test.go`): `NewTelegramHandler`
создавал свой изолированный `identities`-репозиторий, из-за чего
`svc.SetTelegramLocale` и чтение локали в `Handler` работали с разными
хранилищами — почта one шла бы по-русски даже после сохранения `en`.
Починено адресно (хелпер собран через `NewHandler` с общим экземпляром),
без изменения публичного API `NewTelegramHandler`.

Покрытие: новый пакет `locale` — 12 кейсов; `client_test.go` — форматы
на 3 локали + fallback; `bot/handler_test.go` — 4 новых теста (ответ на
en/es, fallback на неизвестном `fr`, персистентность локали между
взаимодействиями); `integration_test.go` — сквозной тест «два получателя,
две локали, одно событие → два разных текста». Все 30+ существующих
Go-тестов остались зелёными без изменений — RU-путь побайтово тот же.

---

<a id="cfg-1"></a>
### CFG-1 🟢 Неиспользуемые mypy-override в `pyproject.toml`

**Проблема.** mypy при каждом запуске сообщает:
`note: unused section(s): module = ['arabic_reshaper.*', 'bidi', 'pypdf.*']`.
Шум в выводе гейта и признак того, что конфиг разошёлся с зависимостями.

**Где.** `pyproject.toml`, блок `[[tool.mypy.overrides]]` со списком
`reportlab/docx/openpyxl/PIL/pypdf/bidi/arabic_reshaper`.

**Как чинить.** Удалить из списка три упомянутых модуля. Остальные
оставить — они используются.

**Важно знать.** Модули из группы `synthetic-data` — опциональная
зависимость, поэтому проверьте с установленным extra
(`pip install -e ".[synthetic-data]"`), прежде чем чистить: список может
отличаться в зависимости от того, что установлено.

**↺ Пересмотрено — правка не нужна, была бы вредной.** Перед удалением
проверил: `import arabic_reshaper`, `from bidi.algorithm import
get_display`, `from pypdf import PdfReader` **реально существуют** в
`utils/synthetic_data/core/`, это не мёртвый код. Разгадка в том, что
каждая запись в overrides дана **двумя формами** — базовой (`"pypdf"`) и
wildcard для подмодулей (`"pypdf.*"`) — а конкретный код в проекте
использует только одну форму на пакет: `import arabic_reshaper` (базовая)
→ `arabic_reshaper.*` простаивает; `from bidi.algorithm import ...`
(subpath) → базовая `bidi` простаивает; `from pypdf import PdfReader`
(базовая) → `pypdf.*` простаивает. Это не мусор конфига, а защитная
избыточность на случай будущих подмодульных импортов. Удаление сломало бы
mypy при малейшем изменении формы импорта в будущем. Проверил и способ
вызова — реальный гейт (`scripts/dev_tools/full_check.py:1747`) зовёт
mypy **точно** тем же набором путей, что я тестировал, включая чистый
`.mypy_cache` — воспроизводится стабильно, это не блажь кэша.

---

<a id="i18n-3"></a>
### I18N-3 ⚪ `/internal/**` полностью по-русски

**Наблюдение, не баг.** Все страницы внутренней ops-консоли содержат
захардкоженные русские строки (`AI-черновики`, `Пересчёт метрик`,
`Пользователи`, `Роль`, подписи форм и т.д.).

**Где.** `apps/web/src/app/internal/**` и `apps/web/src/features/admin-*/**`,
например `apps/web/src/app/internal/users/page.tsx:11`,
`apps/web/src/features/admin-evidence/EvidenceAdminView.tsx:80`.

**Почему это, скорее всего, осознанно.** `apps/web/src/middleware.ts:10-18`
явно выводит `/internal` из-под локального префикса («ops-консоль, никогда
не локализуется») и закрывает её вне dev/local. Это внутренний инструмент
для одной команды.

**Что решить.** Просто зафиксируйте позицию: «внутренняя консоль — только
русский» либо «локализуем и её». Сейчас это нигде не записано, и следующий
разработчик может начать переводить её вразнобой.

**↺ Пересмотрено — уже зафиксировано, просто первый грep не нашёл.**
Решение **уже записано** в `docs/_arch_/08_Открытые_вопросы.md`, пункт
Р-12 (i18n-стратегия, пересмотрено владельцем 2026-07-19): «`/internal/**`
(админка/модерация) **вне области, не тронут**». Мой первый поиск искал
паттерны вида `"internal.*русск"`/`"internal.*локал"`, а формулировка в
доке — «вне области» — под них не попадала. Плюс сам код уже
самодокументирован: `apps/web/src/app/internal/layout.tsx` содержит
комментарий «owner-confirmed scope boundary». Добавлять дублирующую запись
не стал — только создал бы риск расхождения между двумя источниками
истины.

---

<a id="arch-2"></a>
### ARCH-2 ⚪ `apps/worker` не упомянут в compose/CI/доках

**Наблюдение.** Поиск по `docker-compose.yml`, `.github/workflows/`,
каталогу dev-tools-раннера и `docs/` не находит ни одного упоминания
worker'а. Оператор, пришедший в проект, не узнает, что сервис существует и
как его запускать.

**Как чинить.** Достаточно малого: строка в
`docs/_arch_/01_Продукт/02_Текущее_состояние_системы.md` и/или запись в
каталоге `utils/dev_tools_scripts_runner/config/` — чтобы он появился в
`python dev_tools_scripts_runner.py`.

**Важно знать.** Тесты worker'а в CI **гоняются** (они лежат в общем
`tests/`), так что регресс поймается. Проблема ровно в обнаруживаемости.

**✅ Закрыто документацией.** Проверил перед правкой: `worker` **уже**
упоминался в `02_Текущее_состояние_системы.md` (строка 117, «translation
jobs + worker foundation»), но только как факт существования — без
команды запуска. Добавил конкретную строку рядом с другими ручными
периодическими джобами (`outbox_relay.py`, `dispatch_trip_reminders.py`)
с точной командой (`python apps/worker/main.py translation-jobs
[--target-locale …] [--dry-run]`) и пометкой, что раннер/CI его пока не
знают. Регистрацию в `dev_tools_scripts_runner.py` не делал — это
отдельная задача с собственным охватом (новый `ScriptInfo`, проверка, что
не сдвигает позиционные номера в интерактивном меню — см. прецедент с
i18n-parity в истории коммитов), не часть этой находки.

---

## 2. Раньше исправлено в этой же сессии (до просьбы «только список»)

Эти 7 пунктов были исправлены до того, как стало ясно, что нужен только
список для самостоятельной правки. Оставлены как есть (не откатывались) —
и позже владелец явно попросил устранить всё найденное, так что откат не
понадобился бы в любом случае. Уже в `main`, проверены.

| # | Что было | Файл | Коммит |
|---|----------|------|--------|
| F-1 | Шапка строила ссылки в дефолтной локали (`/en/...` на `/ru`) — рассинхрон гидратации + «Войти» вёл на английский логин | `apps/web/src/app/[locale]/layout.tsx` | `0fbf52d` |
| F-2 | Широкие таблицы обрезались карточкой `overflow-hidden` на мобильном — правая колонка недостижима | `packages/ui/src/primitives/DataTable.tsx` | `0fbf52d` |
| F-3 | React #418 (hydration mismatch), воспроизводился **в prod-сборке** на `/glossary`, `/sources`, `/legal-signals`, `/legal-signals/timeline` | + `apps/web/src/shared/ui/ClientOnly.tsx`, 3 view | `7b5ad79` |
| F-4 | Рекурсивный `requestAnimationFrame` без отмены: setState после размонтирования, гонка двух циклов | `Counter.tsx`, `DonutChart.tsx`, `GaugeArc.tsx` | `ca7175e` |
| F-5 | `Counter` игнорировал `prefers-reduced-motion` | `packages/ui/src/primitives/Counter.tsx` | `ca7175e` |
| I-1 | Контейнер notifier работал **от root** (в отличие от api) | `apps/notifier/Dockerfile` | `a372711` |
| D-1 | «55 миграций» вместо фактических 56 | `docs/_arch_/01_Продукт/02_Текущее_состояние_системы.md` | `fa98bc8` |

---

## 3. Проверено и **чисто** — сюда можно не смотреть

Чтобы вы не тратили время на области, где проблем нет:

**Backend** (42 роутера / 100 репозиториев / 136 сервисов): авторизация
навешана на **все** эндпоинты (проверена матрица целиком; «дыры» оказались
type-alias паттерном `_Editor`/`_Contributor`); статусы `suspended`/`deleted`
дают 403 на уровне валидации сессии; IDOR невозможен — `user_id` берётся из
сессии, не из запроса; SQL везде параметризован (f-строки интерполируют
только статические списки колонок); проглоченных `except` — ноль;
транзакции безопасны (пул авто-коммитит, P2-9); нарушений реестра
инвариантов нет.

**OpenAPI-контракт:** 215 путей в живом API = 215 в `contracts/openapi.yaml`,
односторонних путей 0, расхождений по наборам методов 0,
`pnpm contracts:generate` без диффа.

**БД:** 56 миграций — нумерация без пропусков, у всех заголовки, все
`CREATE` идемпотентны, незащищённых `DROP/TRUNCATE/DELETE` нет,
`sqlfluff` чист; иммутабельность подтверждена live (повторный прогон
мигратора — корректный no-op, значит checksum'ы сходятся).

**Notifier:** gRPC-авторизация на всех методах (fail-closed,
constant-time сравнение); доставка **effectively-once** (commit после
обработки + dedup); DLQ для «ядовитых» сообщений; `go vet`/`build`/`test`
зелёные.

**Тесты и статика:** полный `pytest` зелёный; `ruff` чист (709 файлов);
`mypy --strict` чист (707 файлов); vitest 86 (web) + 8 (ui); сборка 47
маршрутов. Отключённых тестов нет — единственный `pytest.skip` в
`tests/repository/conftest.py` это осознанный opt-in, и CI флаг
**выставляет** (`.github/workflows/quality.yml:212`); с ним 29 тестов
проходят.

**Прочее:** ни одного `TODO`/`FIXME`/`HACK` в коде; ни одного
`console.log`; `dangerouslySetInnerHTML` не используется нигде (нет
XSS-поверхности); все внешние ссылки с `rel="noreferrer"`; у всех
`<canvas>` есть aria-разметка; горизонтального overflow нет ни на одном
маршруте ни на desktop, ни на mobile; CI не обращается к `secrets.*` и не
интерполирует `github.event` (нет script-injection).

---

## 4. Что я проверил и **отбросил** как ложные срабатывания

Чтобы вы не наткнулись на то же самое и не потратили время:

- **`Math.random()` в чартах** (`BarColumns`, `DivergingMeter`, `DonutChart`,
  `DriftBoard`, `GaugeArc`, `Heatmap`, `RadarChart`) — выглядит как риск
  гидратации, но весь этот код **внутри `useEffect`** (live-режим
  «дыхания» данных) с корректным `clearInterval`. В рендере ничего
  случайного нет.
- **`target="_blank"` без `rel`** — греп даёт ~10 совпадений, но `rel="noreferrer"`
  во всех случаях стоит **на следующей строке** JSX. Всё безопасно.
- **Поля формы без `aria-label`** — первый прогон дал 22 штуки, но regex
  ломался на `=>` внутри JSX-атрибутов. После корректного разбора осталось
  **14** (они в A-1); например `GlossaryFilters.tsx`, `DecisionWeightSlider.tsx`
  и `CommunityCountryBlock.tsx:219` размечены правильно.
- **«INSERT без ON CONFLICT» в миграциях** — 6 кандидатов, все безопасны:
  `021` защищён блоком `DO $$ ... IF NOT EXISTS`, `010`/`020` — только
  `UPDATE`, `014`/`028`/`055` — только `ALTER`, `006` — delete-then-insert.
- **Мутации «без обработки ошибок»** — обработка есть, но только откат
  кэша. Это не отсутствие обработки, а отсутствие обратной связи — вынесено
  отдельным пунктом UX-1.
- **Hydration на `/countries/russia`** — падало в dev при первой (холодной)
  компиляции маршрута, но в **prod-сборке чисто**. Это артефакт dev-сервера
  Next.js, а не баг приложения. Именно поэтому прогон делался против обеих
  сборок.

---

## 5. Границы аудита — чего я НЕ проверял

Честно, чтобы вы не считали покрытие полным там, где его нет:

- **Скриншот с «наслоением» элементов, который вы упоминали, в чат не
  пришёл.** Коллизии я проверял программно (переполнение по горизонтали на
  двух брейкпоинтах, стек z-index) — чисто. Если проблема визуальная и
  точечная, назовите экран и разрешение — проверю адресно.
- **Динамические маршруты, требующие реальных ID**, в авто-прогон не вошли:
  `/trips/[id]`, `/trips/shared/[token]`, `/authors/[userId]`,
  `/migration-board/[id]`, `/routes/[id]`,
  `/account/country-proposals/[id]`, `/decision/passports/[token]`.
- **Не читал построчно** все 236 файлов `services`/`repositories` и 229
  компонентов `apps/web` — использованы сплошные сканы по рисковым
  измерениям (авторизация, SQL, ошибки, транзакции, утечки, a11y) плюс
  глубокое чтение критичного и авто-прогон всех маршрутов.
- **Не изолирован точный триггер** F-3: почему #418 бил именно те 4 view, а
  `/search` и `/countries` с тем же паттерном — нет. Применённый фикс
  закрывает класс целиком, но первопричина не доказана.
- **`docs/_ideas_and_concepts_/` не открывался** — ваша приватная папка,
  запрещена правилами проекта.
- **Нагрузочное/производительное тестирование не проводилось** — оценка
  производительности только статическая (N+1, размеры бандлов).

---

## 6. Итоговая верификация устранения (2026-07-20)

Каждая правка коммитилась и проверялась отдельно; в конце — сквозной
прогон всего сразу:

- `pnpm typecheck` (web + ui) — чисто.
- `pnpm lint` (web + ui) — чисто.
- `pnpm --filter web test` — 86/86; `pnpm --filter ui test` — 8/8.
- `python scripts/dev_tools/i18n_parity_check.py` — 1046 ключей совпадают
  в `en`/`ru`/`es` (было 1023 — добавился 1 новый ключ `remindAtLabel`).
- `pnpm build` — 48 маршрутов (было 47 — новый `[locale]/[...rest]`),
  `✓ Compiled successfully`.
- `python -m ruff check` / `ruff format --check` — чисто, 709 файлов.
- `python -m mypy` (с полностью очищенным `.mypy_cache`, дважды) —
  `Success: no issues found in 707 source files`.
- `python -m sqlfluff lint database --dialect postgres` — `All Finished!`.
- `docker compose config --quiet` — валиден.
- Полный `pytest` (root `tests/`, без gated repository-интеграционных) —
  зелёный, exit 0.
- `go build ./...`, `go vet ./...`, `go test ./...` (notifier, все 17
  пакетов) — чисто, включая 5 новых тестовых файлов/дополнений.
- Живой прогон в браузере: dev-стек поднят (Postgres/Redis/API в Docker,
  мигрировано, посеяны демо-страны), проверены live — все 5 полей
  Community-блока (`id`↔`label[for]`), группы на Шаге 2 мастера подбора
  (`role="group"`/`aria-labelledby`), 404 на 5 сценариях локали, консоль
  браузера — без ошибок.

**Изменено файлов:** ~35 (apps/web, packages/ui, apps/notifier,
apps/worker, docker-compose.yml, docs/_arch_, task-checklist.md).
**Новых зависимостей:** ноль. **Новых миграций БД:** ноль (правки не
затрагивали схему). **Breaking changes для API-контракта:** ноль.
