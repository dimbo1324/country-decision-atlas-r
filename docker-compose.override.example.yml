# Copy this file to docker-compose.override.yml for local-only overrides.
# Do not commit real secrets or machine-specific paths.

services:
  postgres:
    ports:
      - "5433:5432"

  redis:
    ports:
      - "6379:6379"

  api:
    ports:
      - "8000:8000"
