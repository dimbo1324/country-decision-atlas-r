# Решения владельца и открытые вопросы

> Журнал продуктовых решений владельца и оставшиеся вопросы. Принятые решения перенесены в соответствующие документы (видение, план, реестр инвариантов) — здесь фиксируется первоисточник с датой.

---

## 1. Принятые решения (2026-07-04)

| № | Тема | Решение |
|---|---|---|
| Р-1 | Тестовые страны (RU/UY/AR) | **Не удаляются никогда.** До Эпизода 5 — рабочий тестовый набор. После Эпизода 5 — скрываются с публичных поверхностей и сохраняются как **восстановимый демо-набор**: экспорт в фикстуры (JSON-дамп) + dev-инструмент быстрого восстановления в `scripts/dev_tools/` (регистрируется в оркестраторе). Детали — план, Эпизод 5 |
| Р-2 | Модерация авторских методологий | Премодерация до первых ~50 авторов; критерий перехода к постмодерации: доля отклонений стабильно < 10% |
| Р-3 | Монетизация | Юрисдикция оперирования — **Уругвай**. Модель принята: (а) добровольные донаты платформе без привилегий + (б) донаты авторам с комиссией платформы. Сроки — не определены (не блокер). Кандидаты-провайдеры для изучения в интеграционном транше: dLocal (Монтевидео), Mercado Pago, PayPal |
| Р-4 | Лицензия контрибуций | Вариант (в): данные стран — неисключительная бессрочная лицензия платформе; авторские методологии — выбор автора при публикации (лицензия платформе \| CC BY-SA) |
| Р-5 | Research-папки `03–07` | Сохраняются, структура не трогается |
| Р-6 | Email-канал | Бэклог; ранняя аудитория покрывается Telegram |
| Р-7 | Названия метрик | Нейминг-ревизия при визуальном транше; стиль — **образный** |
| Р-8 | Локаль в URL | Принято: locale-in-URL для публичных страниц; финализация схемы — до визуального транша |
| Р-9 | `CHANGELOG.json` | Не восстанавливается; история задач — git + финальные отчёты |
| Р-10 | Период автономной разработки | До переезда владельца в Уругвай (~3–4 месяца от 2026-07): **никаких реальных внешних подключений** (LLM-API, боевой Telegram, почта, платежи, OAuth). Все эпизоды 1–7 реализуются и тестируются полностью офлайн через fake-швы; боевые подключения — отдельным «интеграционным траншем» после переезда. Везде, где нужно подключение, уже сейчас закладывается шов |
| Р-11 | E2E/смоук-фикстуры vs. демо-набор (P1-9, Аудит-эпизод 8) | **Вариант Б (интерим), Вариант А — после переключения E2E на синтетический пайплайн.** Сейчас: `--visible` по-прежнему временно снимает `is_demo` только в CI/dev-джобах, зафиксировано и протестировано, что дефолтный путь скрипта демо-набор не расшатывает. `utils/synthetic_data/` (точка входа `scripts/synthetic_data.py`, перенесено из `scripts/synthetic_data`/`docs/synthetic_data` — см. `SYNTHETIC_DATA_PLAN.md` Этап 0) дошёл до Этапа 7 (изолированные `dataset_id`-fixtures и quality-gate интеграция реализованы), но сам переключатель E2E/смоуков на синтетический набор и удаление `--visible` из CI — отдельная, ещё не начатая задача (см. `SYNTHETIC_DATA_PLAN.md` Этап 3). Детали — план, Эпизод 8; `utils/synthetic_data/README.md` |
| Р-12 | i18n-стратегия для UI-контента редизайна (зафиксировано 2026-07-18, Этап 5 «Закрепление»; **СУПЕРСЕДЕНО 2026-07-19**, см. ниже) | ~~**Контент ru-only для лейблов интерфейса; `next-intl` остаётся только для «каркаса» приложения.**~~ Не новое решение владельца, а явная фиксация паттерна, который уже сложился де-факто во всех волнах редизайна (Этапы 0-3) и прямо следует из существующего правила `.ai/project/12-domain-rules.md` («не использовать AI-перевод без явного запроса владельца»). Проверено по коду: каталог сообщений `next-intl` (`apps/web/src/messages/{en,ru}.json`, 90 ключей) покрывал только шапку/футер/формы авторизации/палитру поиска/страницы ошибок — ни одного ключа для контента фич или страниц. Все лейблы, добавленные в редизайне (названия табов досье, шаги мастера подбора, лейблы чип-фильтров, подписи каталога) были захардкожены как русские строки, не зависящие от `/en/` vs `/ru/` URL-локали. **Владелец пересмотрел это решение 2026-07-19** (задача `feat/i18n-three-language-interface`, `task-checklist.md`): полная миграция интерфейса на три языка — английский (новая локаль по умолчанию, ранее была русская), русский, испанский — выполнена end-to-end по всем 154 файлам публичной поверхности (`apps/web/src/app/[locale]/**` и рендерящиеся на них feature-компоненты), 10 стадий, итоговый каталог сообщений вырос с 90 до 1023 ключей на локаль (`i18n_parity_check.py`, 3-локальная проверка паритета). Область по-прежнему ограничена **интерфейсом** (chrome + лейблы, навигация, формы, названия табов/шагов) — контентные данные бэкенда (названия стран, названия сценариев, тексты источников, тела правовых сигналов, тексты методологии) остаются в исходном языке независимо от локали интерфейса; для локали `es` они запрашиваются у бэкенда с фолбэком на `en` через новый `toApiLocale()` (бэкендный `LocaleCode` по контракту — по-прежнему только `en`\|`ru`, испанский на уровне данных не планируется). `/internal/**` (админка/модерация) вне области, не тронут. Полное описание границы, найденные и исправленные по ходу баги, и оставшийся техдолг (недостающий `ariaLabel` у `packages/ui`'s `Breadcrumbs` на 4 уже смигрированных страницах; расхождение `Негативное`/`Отрицательное` в двух разных источниках лейблов для одного и того же enum-значения) — см. `task-checklist.md` в корне репозитория. |

## 2. Ещё открытые вопросы

### В-1. Первые модераторы

**Статус:** владелец затрудняется ответить (2026-07-04). **Что нужно:** число модераторов фазы 1 и наличие кандидатов; подтверждение/корректировка порогов допуска (репутация, стаж). **Блокер для:** фазы 1 института модераторов — механизм (Эпизод 3) реализуется независимо; вопрос станет актуален к запуску авторского контура (Эпизод 4, invite-only авторы). До этого владелец закрывает модерацию сам.

### В-2. Хостинг и продакшен-развёртывание

**Статус:** владелец затрудняется ответить (2026-07-04). **Что нужно:** площадка (VPS/облако), регион, бюджет; рекомендация остаётся — один хост с docker-compose на старте достаточен. **Блокер для:** публичного запуска Эпизода 2 (напоминания требуют always-on). Разработку не блокирует: fake-режим доставки полноценно тестируется локально. Естественная точка решения — интеграционный транш; для Уругвая учесть латенси до аудитории (вероятно, ЕС/US-East региона достаточно).

### В-3. Сроки монетизации

**Статус:** не определены (2026-07-04), сознательно. **Блокер:** нет — фундамент (Р-3, Р-4, предохранители в реестре инвариантов §6) закладывается независимо от сроков.
