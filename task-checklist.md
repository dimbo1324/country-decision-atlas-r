# Task: устранить все находки из AUDIT_2026-07-20.md

Владелец: «Устрани все ошибки до единой, которые в AUDIT_2026-07-20.md».
Работа велась прямо в `main` (установленный режим этой сессии для всей
аудит-задачи).

## Находки (9 действий + 2 документационных)

- [+] A-1 — 14 полей форм без доступного имени. `aria-label`/`id`+`label`/
      обёртка `<label>` по месту; новый ключ `remindAtLabel` (en/ru/es).
- [+] A-2 — 8 `FieldLabel` без `htmlFor`. Проп сделан обязательным
      (TS сам нашёл все 8); для 2 группы-заголовков — новый примитив
      `FieldGroupLabel` + `role="group"`/`aria-labelledby`.
- [+] UX-1 — молчаливый откат оптимистичных мутаций. Точечный
      `toast.error` в 4 конкретных `onError` (не глобальный
      `MutationCache` — проверка показала, что дал бы двойные тосты в 15
      уже самостоятельно обрабатывающих файлах).
- [+] I18N-1 — нет локализованной 404. `[locale]/not-found.tsx` +
      `[locale]/[...rest]/page.tsx` (catch-all, без него вложенная
      граница недостижима для произвольных URL — обнаружено live).
- [+] SEC-1 — dev-порты на `0.0.0.0` без auth. 8 портов привязаны к
      `127.0.0.1` (postgres, redis, redpanda×2, mongo, notifier×2);
      `api` намеренно не тронут.
- [-] I18N-2 — хардкод локали в таймстампах админки. **Пересмотрено**:
      `/internal` жёстко на `locale="ru"` по дизайну, правка была бы
      косметикой без функционального смысла.
- [+] ARCH-1 — `sys.path`-хак в worker. Заменён на поиск корня по
      маркеру (`pyproject.toml`) вместо хрупкой цепочки `dirname`.
- [+] NOTIF-1 — Telegram-уведомления только по-русски. Полная
      локализация (весь бот + шаблоны событий) на основе реального
      `language_code` из апдейтов; find en/ru/es каталоги, 5 новых
      тестовых файлов/дополнений.
- [-] CFG-1 — «неиспользуемые» mypy-override. **Пересмотрено**: это
      неиспользуемая половина пары bare/wildcard для реально
      используемых импортов, не мёртвый конфиг.
- [+] ARCH-2 — worker не упомянут в доках. Строка в
      `02_Текущее_состояние_системы.md` с точной командой запуска.
- [-] I18N-3 — позиция по `/internal` не зафиксирована. **Пересмотрено**:
      уже зафиксирована в `08_Открытые_вопросы.md`, Р-12.

## Verification

- [+] `pnpm typecheck` / `lint` (web + ui) — чисто.
- [+] `pnpm --filter web test` 86/86, `pnpm --filter ui test` 8/8.
- [+] `i18n_parity_check.py` — 1046 ключей, en/ru/es совпадают.
- [+] `pnpm build` — 48 маршрутов, чисто.
- [+] `ruff check`/`format --check` — чисто (709 файлов).
- [+] `mypy` с чистым `.mypy_cache`, дважды — `Success: 707 files`.
- [+] `sqlfluff lint database` — `All Finished!`.
- [+] `docker compose config --quiet` — валиден.
- [+] Полный `pytest` (root) — зелёный.
- [+] `go build`/`vet`/`test` (notifier, 17 пакетов) — чисто.
- [+] Live-проверка в браузере: aria-разметка (Community-блок, мастер
      подбора Шаг 2), 404 на 5 сценариях локали, стек поднят и работает
      после смены портов, консоль без ошибок.

## Completion

- [+] `AUDIT_2026-07-20.md` — каждая находка помечена
      ✅ Устранено / ↺ Пересмотрено / ✅ Закрыто документацией, с
      честным описанием того, что реально сделано или почему не сделано.
- [ ] Коммит(ы) + пуш в `main`.
- [ ] Финальный отчёт.
