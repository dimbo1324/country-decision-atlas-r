# Чек-лист задачи: консолидация README для scripts/synthetic_data

Цель: обновить `scripts/synthetic_data/README.md` так, чтобы он стал
самодостаточным справочником (архитектура, модель данных, локали,
профили, CLI, безопасность, тестирование), и владелец мог удалить
`docs/synthetic_data/synthetic_data_pipeline_technical_specification.md`
(изначальный проектный ТЗ-документ) без потери актуальной информации.

Ветка: `docs/synthetic-data-readme-consolidation-v1`.

## Состав работ

- [+] Сверить README с фактическим кодом (не с ТЗ): `world_models.py`,
      `sql_fixture.py`, `world_config.json` (профили/архетипы),
      `locale_corpus.json` (15 локалей), `dataset_packager.py` (структура
      вывода), `cli.py` (полный список команд/флагов), `paths.py`.
- [+] Переписать README: назначение, правила безопасности/non-goals,
      архитектура (один канонический мир), модель данных, локали,
      профили/архетипы, структура вывода, SQL-fixtures и гарантии
      безопасности, полный CLI-референс (world + legacy), детерминизм,
      тестирование (включая явную регистрацию в quality gate), известные
      ограничения.
- [+] Удаление `docs/synthetic_data/synthetic_data_pipeline_technical_specification.md`
      — выполнено владельцем; попутно поправлена единственная оставшаяся
      живая ссылка на файл (`docs/_arch_/08_Открытые_вопросы.md`, Р-11) —
      указывает на README вместо удалённого документа.

## Проверка

- [+] Markdown: сбалансированы code fences, нет очевидных ошибок разметки.
- [+] Факты сверены с кодом на момент написания (не переписаны из ТЗ
      бездумно): число тестов (197), 4 таблицы в SQL-fixture, 5 профилей,
      5 архетипов, 15 локалей — все подтверждены прямым чтением кода/данных.

## Завершение

- [+] Чек-лист заполнен `+`/`-`.
- [+] Коммит на ветке. Merge в `main` (fast-forward) и push на
      `origin/main` — выполнено по явному подтверждению владельца.
