# Чек-лист задачи: Аудит-эпизод 1 — `fix/db-index-safety-v1`

Цель: устранить P0-1 (`колонка::text = %s` — 187 вхождений, отключает индексы)
и связанные находки того же участка кода (P3-1, P3-2, P1-8, P2-14, P3-9) —
см. `docs/_arch_/09_План_устранения_аудита.md`, раздел «Аудит-эпизод 1».
Поведение системы не меняется — меняется только форма SQL и число запросов
на путь аутентификации.

## Подготовка

- [ ] Собран полный список вхождений `::text = %s` по `apps/api/app/repositories/`,
      исключены случаи, где колонка реально `text` (не uuid).

## Реализация

- [ ] P0-1: `колонка::text = %s` → `колонка = %s::uuid` по всем репозиториям.
- [ ] Тест-инвариант: `::text = %s` запрещён в `apps/api/app/repositories/`.
- [ ] P3-1: `common.py`/`countries.py` — `get_by_id`/`get_by_slug` разведены,
      публичные эндпоинты используют только slug.
- [ ] P3-2: `auth.py:343` — `fetch_all()` → `execute()` для UPDATE.
- [ ] P1-8: `touch_session` — запись раз в N минут, а не на каждый запрос.
- [ ] P2-14: `validate_session_token` — один JOIN-запрос вместо 3-4.
- [ ] P3-9: path-параметры `*_id` типизированы как `UUID` на границе роутера.

## Проверка

- [ ] Полный `pytest tests/` зелёный без правок ожиданий (поведенческий noop).
- [ ] Новый grep-тест-инвариант проходит.
- [ ] Полный quality gate (`python dev_tools_scripts_runner.py`) зелёный.
- [ ] `EXPLAIN` на 1-2 горячих запросах (сессия, capability) до/после — в отчёте.

## Завершение

- [ ] Чек-лист заполнен `+`/`-`.
- [ ] Статус эпизода AE-1 отмечен в `docs/_arch_/09_План_устранения_аудита.md`.
- [ ] Финальный отчёт написан.
- [ ] Merge/push — только по явному запросу владельца.
