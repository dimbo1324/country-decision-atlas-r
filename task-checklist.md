# Task: Сплошной аудит ВСЕГО репозитория по слоям (2026-07-20)

Владелец попросил полный аудит, устранить ошибки/коллизии перед ручным
тестированием, оформить в `.md` и запушить. Работа велась прямо в `main`
по прямому указанию владельца.

Дважды по ходу владелец справедливо спрашивал, весь ли проект покрыт.
Оба раза честный ответ был «нет», и охват расширялся: сначала с
прицельного прохода (~5–10%) до 4 слоёв приложения, затем до **10 слоёв —
всего репозитория**.

## Слои

- [+] **1. Backend** (42 роутера / 100 репо / 136 сервисов) — матрица auth
      по всем роутерам, фундамент авторизации построчно, SQL, ошибки,
      транзакции, N+1, инварианты. **Багов нет.** `4287973`
- [+] **2. Frontend** — авто-прогон всех 36 маршрутов (desktop+mobile,
      dev И prod). **3 бага исправлено** (F-1 локаль в шапке, F-2 клиппинг
      таблиц, F-3 hydration #418 на 4 страницах). `0fbf52d`, `7b5ad79`,
      `638e3f3`
- [+] **3. Notifier (Go)** — gRPC-auth, delivery-семантика, RealClient;
      оба прежних замечания **закрыты**. **Багов нет.** `6ba943b`
- [+] **4. БД** (56 миграций) — нумерация, идемпотентность, sqlfluff,
      иммутабельность/дрейф live. **Багов нет.** `a69dc09`
- [+] **5. Worker + инфраструктура** — worker смоук-тест + 30 тестов;
      **исправлено: notifier-контейнер работал от root**; CI чист.
      `a372711`
- [+] **6. packages/ui** (103 компонента) — **исправлено 2 бага**:
      незакрываемые rAF-циклы в 3 компонентах, reduced-motion в Counter.
      `ca7175e`
- [+] **7. OpenAPI-контракт** — 215 путей в API = 215 в контракте,
      расхождений 0. **Дрейфа нет.** `fa98bc8`
- [+] **8. scripts/utils** — ruff (709 файлов) + mypy strict (707) чисто.
- [+] **9. tests** — полный pytest зелёный; отключённых тестов нет;
      repository-тесты — осознанный opt-in, в CI включены, 29 зелёных.
- [+] **10. Дрейф доков** — **исправлено**: «55 миграций» → 56.

## Итог: 7 исправлений

F-1 локаль в шапке · F-2 клиппинг таблиц на мобильном · F-3 hydration
#418 на 4 страницах · F-4 rAF-утечки в UI-ките · F-5 reduced-motion ·
I-1 root в notifier-контейнере · D-1 дрейф доков.

Backend, Notifier, БД, контракт, scripts/utils, тесты — **без багов**.

## Verification (финальная)

- [+] `ruff check` + `ruff format --check` — чисто (709 файлов).
- [+] `mypy --strict` — чисто (707 файлов).
- [+] Полный `pytest` — зелёный.
- [+] `pytest tests/repository` с флагом + живой БД — 29 зелёных.
- [+] `go vet` / `go build` / `go test` (notifier) — зелёные.
- [+] web+ui `typecheck` / `lint`; vitest 86+8; `pnpm build` 47 маршрутов.
- [+] `sqlfluff lint database` — чисто.
- [+] Прогон 36 маршрутов в prod — без единой проблемы.
- [+] Контракт: 215 = 215, дрейфа нет.
- [+] Docker-образ notifier пересобран, `id` → `uid=100(app)`.

## Not done / honest gaps

- [-] **Картинка с «наслоением» от владельца в чат не пришла.** Коллизии
      проверены программно (overflow на 2 брейкпоинтах, z-index) — чисто.
      Адресная проверка конкретного экрана — по указанию.
- [-] **Не применено намеренно:** привязка портов dev-стека
      (redis/mongo без auth, postgres `change-me`) к loopback вместо
      `0.0.0.0`. Это решение по workflow владельца — фикс однострочный,
      применю по слову.
- [-] Динамические маршруты с реальными ID (`/trips/[id]`,
      `/authors/[userId]`, паспорта) в авто-прогон не вошли.
- [-] Не читал построчно все 236 файлов services/repositories и 229
      компонентов web — использованы сплошные сканы рисковых измерений,
      глубокое чтение критичного и авто-прогон всех маршрутов.
- [-] Не изолирован точный триггер, почему #418 бил именно 4 view, а не
      `/search`+`/countries` с тем же паттерном (фикс закрывает класс).
- [-] `docs/_ideas_and_concepts_/` не читался — приватная папка владельца
      (запрещено правилами проекта).

## Completion

- [+] 10 коммитов, все запушены в `main`.
- [+] Отчёт: `AUDIT_2026-07-20.md` в корне.
