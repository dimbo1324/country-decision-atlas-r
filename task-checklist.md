# Чек-лист задачи: конвейер синтетических данных — Этап 7
# (интеграция в рабочий процесс и выпуск первой версии)

Источник: `docs/synthetic_data/synthetic_data_pipeline_technical_specification.md`,
раздел 23, «Этап 7 — интеграция в рабочий процесс и выпуск первой версии».
Зависимости (Этапы 1-6) уже реализованы и смержены в `main`.

Ветка: `feat/synthetic-data-stage7-workflow-integration-v1`.

## Подготовка

- [ ] Сверить состояние: `git status`, `git log --oneline -10`, читка
      технического задания (раздел 23, Этап 7) и текущего `scripts/synthetic_data`.
- [ ] Подтвердить, что тесты `scripts/synthetic_data/tests` (194 теста) зелёные
      как baseline до начала работы.

## Реализация

- [ ] Завершить/подтвердить CLI-команды `validate`, `plan`, `generate`,
      `render`, `package` — обратная совместимость legacy-режима не нарушена.
- [ ] Явно включить тесты `scripts/synthetic_data/tests` в quality gate
      (`scripts/dev_tools/full_check.py`), поскольку `pyproject.toml`
      (`testpaths = ["tests"]`) их не собирает по умолчанию (раздел 18 ТЗ).
- [ ] Добавить проверки времени выполнения и размера пакета (smoke-тест на
      полный `generate --formats all`).
- [ ] Добавить проверку понятности ошибок CLI (несуществующий `--dataset`
      даёт причину и список известных id).
- [ ] Подготовить/подтвердить небольшой фиксированный smoke-набор для быстрых
      тестов и расширенные профили для ручного тестирования.
- [ ] Провести тестовый запуск в чистом локальном Docker-окружении: generate
      → load-sql → проверка данных → cleanup-sql, без реальных внешних
      интеграций.
- [ ] Сверить фактический результат с ТЗ и архитектурными инвариантами;
      обновить статус Этапа 7 в самом ТЗ (раздел 23).

## Проверка

- [ ] `python -m ruff check`/`ruff format --check` чистые.
- [ ] `python -m mypy scripts/synthetic_data tests` чистый.
- [ ] `py -3.12 -m pytest scripts/synthetic_data/tests -q` зелёный.
- [ ] `py -3.12 -m pytest tests/ -q` (весь проект) зелёный.
- [ ] Полный quality gate (`python dev_tools_scripts_runner.py`) зелёный
      (кроме заранее задокументированного `go test -race`
      Windows/cgo-ограничения, не связанного с этой задачей).

## Завершение

- [ ] Чек-лист заполнен `+`/`-`.
- [ ] Финальный отчёт написан.
- [ ] Коммит(ы) на ветке. Merge в `main` (fast-forward) и push на
      `origin/main` — как только все проверки зелёные (владелец дал явное
      предварительное разрешение на это в рамках текущей задачи).
