# Чек-лист задачи: конвейер синтетических данных — Этап 1 и Этап 2

Цель: доделать начатую работу на ветке `feat/synthetic-world-foundation-v1` —
довести Этап 1 («Основание») и Этап 2 («Страны и временная динамика») до
буквального соответствия
`docs/synthetic_data/synthetic_data_pipeline_technical_specification.md`
(разделы 17 и 23). Этапы 3+ (пользователи/контент/сценарии, i18n, рендеринг
документов, SQL-fixtures) — вне объёма этой задачи.

## Подготовка

- [+] Изучено техническое задание целиком (23 раздела).
- [+] Изучено текущее состояние кода на ветке: `world_models.py`,
      `world_input.py`, `world_generator.py`, `world_validation.py`,
      `seed.py`, `cli.py` (команды `validate`/`plan`/`generate`),
      `world_config.json`, существующие тесты — уже покрывают большую часть
      Этапов 1–2.
- [+] Обнаружены и подтверждены запуском тестов (`py -3.12 -m pytest
      scripts/synthetic_data/tests/`) — все 54 существующих теста зелёные.

## Найденные пробелы относительно буквального текста ТЗ

- [ ] Раздел 6.1/17/23: канонические модели должны быть
      «JSON Schema/Pydantic-модели» — сейчас обычные `@dataclass`, без
      JSON Schema и без полей `supported_locales`/`fictional_notice` в
      самих метаданных (сейчас `fictional_notice` только синтезируется в
      `to_dict()`).
- [ ] Раздел 23, Этап 1, п.6: «проверка отсутствия реальных персональных
      данных, production-настроек и запрещённых внешних вызовов во входных
      данных» — сейчас проверяются только запрещённые названия стран,
      общей проверки на PII/секреты/URL нет.
- [ ] Раздел 23, Этап 1, критерий: «одинаковые конфигурация и seed дают
      одинаковый результат после исключения технического времени создания»
      — текущий тест сравнивает `to_dict()` целиком, включая `generated_on`
      (`date.today()`), совпадение сейчас держится на том, что оба вызова
      происходят в один день, а не на явном исключении поля.
- [ ] Раздел 7.2/14.2: «отрицательный тренд не может быть заявлен без хотя
      бы одного события или источника» — сейчас это верно только по
      построению генератора, нет явного правила в валидаторе, которое ловило
      бы нарушение.
- [ ] Раздел 14.2/23 Этап 2 критерий: «валидатор отклоняет реальные
      названия» — проверка в коде валидатора есть, но нет прямого
      юнит-теста именно на этот путь (только на генератор, который и так их
      не создаёт).

## Реализация

- [ ] Перевести `world_models.py` на `pydantic.BaseModel` (frozen), добавить
      `supported_locales` и `fictional_notice` как настоящие поля
      `WorldMetadata`; сохранить текущую форму `to_dict()`.
- [ ] Добавить тест на `SyntheticWorld.model_json_schema()` — схема
      «зафиксирована» и содержит ожидаемые верхнеуровневые поля.
- [ ] Обновить `test_world_validation.py` под Pydantic (`model_copy(update=…)`
      вместо `dataclasses.replace`).
- [ ] Добавить `core/input_safety.py` (или функцию в `world_input.py`):
      проверка всех строковых значений входной конфигурации на признаки
      PII/секретов/внешних адресов (email, `http(s)://`, `password`,
      `token`, `secret`, `api_key` и т.п.); подключить в `load_world_input`.
- [ ] Сделать `generated_on` явно исключаемым/инъецируемым для сравнения
      воспроизводимости; добавить тест, который не полагается на совпадение
      календарной даты.
- [ ] Добавить в `world_validation.py` правило: значимое изменение метрики
      (снапшот → снапшот) обязано иметь событие с тем же `metric`;
      добавить тест, ловящий нарушение.
- [ ] Добавить прямой юнит-тест `validate_world` на отклонение мира с
      запрещённым названием страны.

## Проверка

- [ ] Полный прогон `py -3.12 -m pytest scripts/synthetic_data/tests/ -q`
      зелёный (существующие + новые тесты).
- [ ] `py -3.12 -m mypy scripts/synthetic_data` чистый.
- [ ] `py -3.12 -m ruff check scripts/synthetic_data` чистый.
- [ ] Ручной прогон `validate`, `plan`, `generate` — без падений.
- [ ] Полный quality gate (`python dev_tools_scripts_runner.py full-check`)
      зелёный.

## Завершение

- [ ] Чек-лист заполнен `+`/`-`.
- [ ] Финальный отчёт написан.
- [ ] Коммит, merge в `main` (ff-only), push на `origin/main` — по прямому
      запросу владельца (уже дан).
