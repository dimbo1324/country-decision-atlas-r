# Task: Сплошной аудит проекта по слоям (2026-07-20)

Владелец попросил полный аудит всех слоёв, устранить ошибки/коллизии перед
ручным тестированием, оформить результаты в `.md` в корне и запушить.
Работа велась прямо в `main` по прямому указанию владельца.

После первого (прицельного) прохода владелец спросил, весь ли проект
проверен. Честный ответ был «нет, ~5–10%», после чего выбрана стратегия
**«по слоям, последовательно»**: каждый слой — отдельный коммит с отчётом.

## Слой 1 — Backend (42 роутера / 100 репозиториев / 136 сервисов)

- [+] Матрица auth по **всем** роутерам; «дыры» оказались type-alias
      паттерном (`_Editor`, `_Contributor`) — все эндпоинты защищены.
- [+] Фундамент авторизации прочитан построчно (rbac, сессии, статусы
      suspended/deleted → 403).
- [+] SQL: только статические allowlist'ы в f-string, значения
      параметризованы — инъекций нет.
- [+] Ошибки: ноль проглоченных `except`. Транзакции: пул авто-коммитит
      (P2-9) — потери данных нет. N+1: только в офлайн batch-джобе.
- [+] Инварианты (деньги→права, репутация→авто-выдача) — не нарушены.
- [+] **Багов не найдено.** Коммит `4287973`.

## Слой 2 — Frontend (все 36 достижимых маршрутов)

- [+] Написан авто-прогон (Playwright) по всем маршрутам на desktop+mobile
      с перехватом console/hydration/crash/overflow.
- [+] Прогон против **dev и prod** — это отделило реальные баги от
      dev-артефактов.
- [+] F-1 (шапка, локаль в `NextIntlClientProvider`) — исправлено.
- [+] F-2 (клиппинг таблиц `DataTable` на мобильном) — исправлено.
- [+] F-3 (React #418 на glossary/sources/legal-signals/timeline) —
      исправлено через `shared/ui/ClientOnly.tsx`.
- [+] Досье `/countries/russia` — оказалось **dev-only** артефактом
      (в prod чисто), не баг приложения.
- [+] Проверено: нет `dangerouslySetInnerHTML`, все внешние ссылки с
      `rel="noreferrer"`, нет горизонтального overflow.
- [+] Финальный прогон 36 маршрутов в prod — **без единой проблемы**.
- [+] Коммиты `7b5ad79`, `638e3f3`.

## Слой 3 — Notifier (Go, 41 файл)

- [+] gRPC-авторизация: interceptor на всех методах, fail-closed,
      constant-time сравнение.
- [+] Доставка: commit-after-process + dedup = effectively-once —
      прежнее замечание «at-most-once» **закрыто**.
- [+] RealClient реализован (реальный вызов Telegram API) — прежнее
      замечание «stub» **закрыто**.
- [+] `go vet` / `go build` / `go test ./...` — всё зелёное.
- [+] **Багов не найдено.** Коммит `6ba943b`.

## Слой 4 — БД (56 миграций)

- [+] Нумерация последовательная, заголовки у всех, все `CREATE`
      идемпотентны, нет незащищённых `DROP`/`TRUNCATE`/`DELETE`.
- [+] `sqlfluff lint` — чисто.
- [+] 6 кандидатов «INSERT без ON CONFLICT» разобраны — все безопасны.
- [+] Иммутабельность/дрейф проверены live: применение на чистую БД +
      повторный запуск = корректный no-op.
- [+] **Багов не найдено.** Коммит `a69dc09`.

## Verification (финальная)

- [+] `pnpm typecheck` / `lint` — чисто (web + ui).
- [+] `pnpm build` — 47 маршрутов, exit 0.
- [+] vitest: 86 (web) + 8 (ui) — зелёные.
- [+] `go vet` / `go build` / `go test` (notifier) — зелёные.
- [+] `sqlfluff lint database` — чисто.
- [+] `pnpm contracts:generate` — без диффа (контракт синхронен).

## Not done / honest gaps

- [-] Приложенная владельцем картинка с «наслоением» в чат **не пришла** —
      коллизии проверены программно (overflow/z-index) на обоих
      брейкпоинтах; адресная проверка конкретного экрана — по указанию.
- [-] Не делалось построчного чтения всех 236 файлов services/repositories
      и 229 компонентов — использованы сплошные сканы рисковых измерений +
      глубокое чтение критичного + авто-прогон всех маршрутов.
- [-] Динамические маршруты, требующие реальных ID (`/trips/[id]`,
      `/authors/[userId]`, `/migration-board/[id]`, `/routes/[id]`,
      паспорта) в авто-прогон не вошли.
- [-] Не изолирован точный триггер, почему #418 бил именно 4 view, а не
      `/search`+`/countries` с тем же паттерном (фикс устраняет класс
      целиком, поэтому это не блокер).
- [-] Полный `dev_tools_scripts_runner.py` (Docker-смоуки, E2E) не гонялся
      целиком — запускались целевые проверки по слоям.

## Completion

- [+] 6 коммитов, все запушены в `main`.
- [+] Отчёт: `AUDIT_2026-07-20.md` в корне.
